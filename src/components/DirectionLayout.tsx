'use client';

export default function DirectionLayout({ children }: { children: React.ReactNode }) {
  // Direction C is the only supported direction
  return (
    <>
      {children}
    </>
  );
}
